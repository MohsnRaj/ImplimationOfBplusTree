import BplusTree from './BplusTree';


// React component to visualize the B+ tree
const BPlusTreeVisualization = () => {
    return (
        <div>
            <h1>B+ Tree Visualization</h1>
            <BplusTree />
            {/* Render the BPlusTree component */}
        </div>
    );
};

export default BPlusTreeVisualization;
